# Red_team-Lab
## How to Build
```
$ docker compose up -d --build
```
The goal of this practice material is to read flags with root privileges in the following order: RECON → SQLI → Login as Admin → RCE → LPE → Flag.
